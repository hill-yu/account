# 项目开发与维护强制规则

本文件适用于 `adx-mid-platform` 中所有后续开发、修复、重构、配置和数据库变更任务。执行任务前先阅读 [`docs/system-maintainer-onboarding-guide.md`](docs/system-maintainer-onboarding-guide.md) 的“1.1 强制开发治理流程”和第 22 节变更记录。

1. 先检查现状、影响范围、相关测试和 SOP，再形成可执行实施方案；每项改动必须说明内容、原因、方案、预期后果、影响范围、验证标准和回滚方式。
2. 所有修改先在本地完成并运行针对性测试。不得直接编辑生产服务器源码。
3. 涉及 Google 数据拉取、OAuth、代理、Google Ad Manager SOAP、采集 runtime 或调度的真实测试，必须先明确指定获授权的测试账号及其测试代理；除非用户明确授权，不得使用生产账号、生产代理或批量调度进行测试。
4. 修改后的代码必须先经独立审阅。仅当审阅无阻塞问题，或阻塞问题已修复并复审通过，才可进行集成/真实账号测试、提交 Git 或部署。审阅必须覆盖差异、测试、迁移、错误处理和安全边界。
5. 每次功能或代码变更必须追加记录到 `docs/system-maintainer-onboarding-guide.md` 第 22 节；记录不得省略变更内容、原因、实施方案、实施结果/后果、影响范围、测试、审阅结论、Git 提交号和发布状态。
6. 部署前，全部发布改动（代码、测试、迁移、配置样例、文档记录）必须已提交 Git。不得以未提交工作区发布，不得提交任何密码、Token、OAuth 凭据、代理完整凭据或生产数据。
7. 发布前准备备份、迁移顺序、灰度范围、健康检查和回滚方案；发布后记录验证结果。遇到异常立即停止扩大范围，并依据回滚方案处置。
8. 保留用户已有的工作区修改，不做无关格式化、回滚或覆盖。任何生产写操作先备份、后执行、再验证。
9. 分支与目录：`master` 是新服务器唯一发布分支和正式集成基线；日常开发必须从最新 `origin/master` 建立独立任务分支及独立 worktree，任务分支默认使用 `codex/` 前缀。目录名称、历史约定或旧 `dev` 分支不能替代 Git 分支事实；`D:\code\adx-mid-platform-oauth-remediation` 与 `dev` 仅作为已有历史任务的兼容工作区，不再是新任务强制入口。测试、独立审阅和 Git 提交完成后，任务分支方可通过受控合并或精确 cherry-pick 集成至 `master`。新服务器仅从 `master` 同步，禁止直接编辑运行目录。
10. 旧服务器仅作为隔离测试环境：必须使用独立测试服务、数据库、域名、测试账号和测试代理；不得恢复生产 scheduler、正式采集任务或生产写入。
11. 生产发布不得直接使用未核验的 `rsync --delete` 覆盖运行目录；发布前先以完全相同的排除清单运行 `rsync --dry-run`。必须备份并显式排除 `.env`、`control_plane.db`、`control_plane.db-wal`、`control_plane.db-shm`、备份目录和虚拟环境。同步后先检查 systemd `EnvironmentFile` 路径可读、执行 Alembic、验证 Web `/health`，最后才允许启动 scheduler。
12. 每个 systemd 单元的 `WorkingDirectory`、`EnvironmentFile`、`ExecStart` 和运行用户必须与服务器实际路径逐项核对；模板默认值不得替代线上事实。若恢复环境文件或 Token，必须同时验证服务健康和所需配置项存在，且不得在日志、文档或 Git 中输出密钥。
13. 服务器代码一致性：严禁在服务器 Git 克隆目录、运行目录或容器内直接修改并部署源码。任何例外处置必须先将完整差异安全导出到本地隔离分支，完成测试、独立审阅并提交 Git；随后合并至 `master`，只能从该已提交的 `master` 提交发布。每次发布须在台账记录源提交号、目标提交号、Git 工作树路径、运行目录路径和受控文件清单；发布后必须校验运行目录受控源码与目标提交逐文件一致。发现二者不一致时，立即停止覆盖式发布，先建立服务器基线恢复提交并完成审阅，禁止以本地代码直接覆盖。
14. 启动门禁：每次打开新的 Codex 窗口、创建新对话或开始任何新任务时，第一项项目操作必须从当前工作区向上定位并完整读取实际生效的仓库根目录 `AGENTS.md`；随后读取 `docs/问题记录.md`、`docs/system-maintainer-onboarding-guide.md` 的“1.1 强制开发治理流程”和第 22 节。完成读取前，只允许定位仓库根目录，不得执行代码修改、Git 写操作、生产连接或其他项目操作。不得依赖聊天摘要、记忆或其他 worktree 的同名文件替代当前工作区规则。
15. 启动状态报告：完成上述读取后，必须先核验并报告当前仓库根目录绝对路径、worktree 路径、当前分支、HEAD、工作区是否干净、`origin/master` 最新提交，以及当前 HEAD 相对 `origin/master` 的 ahead/behind 状态。若工作区有既有修改、分支来源不明或未取得最新远程信息，必须先保护并厘清，不得直接覆盖、合并、发布或执行生产写操作。
16. 文档统一版本：远程 `origin/master` 是 `AGENTS.md`、`docs/system-maintainer-onboarding-guide.md` 和 `docs/问题记录.md` 的唯一正式事实基线。任务 worktree 中的新增规则和追加记录只属于该任务分支，必须与相关代码一起经审阅、提交并通过 merge、rebase 或精确 cherry-pick 集成回 `master` 后，方可视为全项目正式版本。新 worktree 必须从已更新的 `origin/master` 创建；既有 worktree 不会自动同步，继续任务前必须显式同步并处理冲突。追加式文档冲突必须人工保留双方记录，禁止复制目录、整文件覆盖或以某个 worktree 的版本替换其他记录。
