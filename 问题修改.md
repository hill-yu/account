# 问题修改记录

本文件记录本项目每次代码修改对应的问题、原因、影响范围和验证结果。任何运行代码、测试、配置模板或迁移脚本的修改，都必须在同一提交中更新本文件。

## 2026-07-31：生产实现与旧测试漂移

### 修改问题

从生产服务器同步源码后，后端测试有 10 项失败，collector 测试有 17 项失败。失败测试仍按旧版远端 snapshot 日报、旧 Ad Manager 报表列、旧灰度名单和旧数据表集合进行断言，无法作为 OAuth 凭据整改的可靠回归基线。

### 问题原因

生产环境曾直接修改运行代码，但对应测试没有同步更新。生产代码已经具备以下行为：

- 中台日报接口读取本地 `account_daily_reports` 和 `site_daily_reports` 权威入库结果。
- 权威日报查询使用 `DATE` 维度和 `PUBLISHER` 时区。
- 小时报表包含国家、广告单元和 `AD_EXCHANGE_TOTAL_REQUESTS`。
- 日报与小时数据模型均包含 `requests`。
- 小时覆盖结果包含日报与小时聚合值比对字段。
- 自动日报按当前生产灰度名单和停拉名单执行，普通日报聚合行不代表权威日报任务已经成功。
- 元数据已经包含 `fetch_schedules` 和 `account_report_day_statuses`。

### 修改内容

- 更新 collector SOAP 查询、CSV 输入和解析结果测试，覆盖日报 `PUBLISHER` 口径及小时新增维度。
- 更新日报服务和 fetcher 测试，补齐 `requests`、小时来源时区及覆盖式小时 batch 参数。
- 更新中台日报接口测试，写入本地权威日报并禁止测试期间调用节点 snapshot。
- 更新自动日报与 targeted-recent 测试账号，匹配当前生产灰度和停拉集合。
- 更新小时覆盖和模型元数据断言。

### 影响范围

仅修改测试与本记录文件，不修改任何生产运行代码、数据库结构或部署配置。

### 修改结果

- 前端：`16 passed`
- 后端：`66 passed`
- collector：`73 passed`

三套现有测试恢复全绿，可作为后续 OAuth 凭据统一控制面整改的基线。

## 2026-07-31：缺少版本化 OAuth 凭据、账号拉取策略和审计模型

### 修改问题

旧版只在 `oauth_app_configs` 和节点 MySQL 中保存当前 token，没有凭据版本、待验证版本、统一账号拉取策略或 OAuth 操作审计，无法安全实现 staged 验证、熔断恢复和灰度/停拉互斥。

### 问题原因

原数据模型按单次授权和双端配置设计，未把凭据生命周期与业务拉取策略建模。授权失败后只能修改静态名单和多个 token 副本，无法通过数据库约束保证状态一致性。

### 修改内容

- 新增 `oauth_credentials`，保存加密凭据版本及 `staged/active/retired/rejected/revoked` 生命周期。
- 对 `(oauth_app_id, version)` 建立唯一约束，并通过 SQLite/PostgreSQL 条件唯一索引限制每个 OAuth App 最多一个 active 和一个 staged 版本。
- 新增 `collector_account_policies`，保存生命周期、灰度、小时、权威日报、手工拉取、停拉原因及恢复快照。
- 增加数据库检查约束，禁止 `gray_enabled=true` 与 `exclusion_reason` 同时存在。
- 新增 `oauth_events` 脱敏审计表，不包含 code、callback URL、client secret、refresh token 或 access token 字段。
- 扩展 `oauth_app_configs`，区分 OAuth 流程状态与运行健康状态，并记录当前/待验证凭据版本和失败信息。
- 新增 Alembic `20260731_0012` schema-only 迁移，不在迁移中读取、复制或改写任何秘密。

### 影响范围

影响后端 SQLAlchemy 元数据和数据库 schema。现有明文凭据字段暂时保留兼容，本次尚未切换运行链路，也不会改变当前数据拉取行为。

### 修改结果

- 模型及数据库测试：`11 passed`
- 后端全量测试：`70 passed`
- 临时 SQLite：`upgrade head -> downgrade 20260708_0011 -> upgrade head` 成功，最终 revision 为 `20260731_0012 (head)`。
- 已知历史风险：`alembic check` 仍报告 `site_daily_reports` 的旧索引名和唯一约束与当前模型漂移；该问题早于本次迁移且不属于 OAuth 修改范围，本次未混入修复。

## 2026-07-31：OAuth 凭据缺少应用层加密和安全指纹

### 修改问题

现有控制面以明文保存 client secret 和 refresh token，也没有安全的 token 指纹。日志排查、版本比对和数据库泄露场景都存在秘密暴露风险。

### 问题原因

旧实现直接把 Google token payload 写入模型，没有统一的加解密边界，也没有将“可逆保存”和“不可逆识别”使用不同密钥分离。

### 修改内容

- 新增 `CredentialCipher`，使用 Fernet 对 client secret 和 refresh token 进行可逆加密。
- 使用独立 HMAC-SHA256 密钥生成稳定指纹，不对 token 做可枚举的裸 hash。
- 新增 `ADX_COLLECTOR_CREDENTIAL_ENCRYPTION_KEY` 和 `ADX_COLLECTOR_CREDENTIAL_FINGERPRINT_KEY` 配置。
- 任一所需密钥缺失或密文无效时失败关闭，异常只返回固定错误码，不回显输入、密文或密钥。
- `CredentialCipher.__repr__` 只显示密钥是否配置，不显示密钥内容。
- requirements 显式增加 `cryptography` 依赖，并为后续凭据、策略和审计测试提供集中 fixture。

### 影响范围

新增后端加密基础设施和配置项；当前运行链路尚未调用该服务，因此不会立即改写或读取现有 token。后续迁移和运行时切换前必须配置两把独立密钥。

### 修改结果

- 凭据加密专项测试：`6 passed`
- 后端全量测试：`76 passed`
- 测试确认密文不包含明文、指纹稳定、缺密钥失败关闭、对象和异常文本不泄露秘密。

## 2026-07-31：灰度和停拉策略散落在代码常量且拉取入口缺少统一拦截

### 修改问题

自动日报依赖代码内灰度/停拉常量，手工任务、定向回补、任务认领、batch 入库和终态提交没有统一校验。账号被停拉后，旧任务或其他入口仍可能继续访问 Google 或写入数据。

### 问题原因

历史实现把“选择哪些账号”和“账号当前是否允许拉取”分别散落在 scheduler、service 和静态元组中，没有单一策略函数，也没有凭据版本检查。

### 修改内容

- 新增 `assert_fetch_allowed`，统一校验账号生命周期、停拉原因、灰度/小时/日报/手工开关、OAuth runtime health、active/staged 凭据及版本。
- `oauth_credential_validate` 仅允许当前 staged 版本；`oauth_health_check` 仅允许受控状态和当前 active 版本。
- 将策略门接入 operator task、manual hourly、targeted recent、automatic hourly、automatic daily、claim、batch 和 terminal status。
- 自动日报和默认 targeted-recent 账号集合改为查询 `collector_account_policies`，不再读取静态灰度/停拉集合。
- `invalid_grant`、`manual` 等停拉状态统一拒绝所有普通拉取入口；历史日报查询保持可读。
- 虚拟端到端流程显式写入健康 managed credential 与 policy。新增测试专用 managed-credential stub 开关，默认 `false`，仅虚拟流程设置为 `true`，后续 direct collector 凭据切换完成后移除。
- 旧业务测试使用测试层 gate bypass 以继续聚焦原行为；策略专项测试不使用 bypass，并覆盖全部入口类型和默认拒绝分支。生产代码中不存在 bypass。

### 影响范围

影响后端任务创建、自动调度、任务认领、batch 入库和状态提交。数据库未完成 policy/credential 迁移的账号会默认被拒绝，因此上线前必须先执行后续迁移工具并检查覆盖率。

### 修改结果

- 策略、调度、路由和虚拟流程专项测试：`61 passed`
- 后端全量测试：`87 passed`
- 停拉账号的历史日报查询测试通过，确认 acquisition policy 不影响只读接口。

## 2026-07-31：OAuth 重授权会重复签发令牌且 callback 直接覆盖运行凭据

### 修改问题

旧版授权链接固定携带 `prompt=consent`，健康账号也可以无确认地反复发起授权。callback 成功后会立即把新 refresh token 和短期 access token 写回当前 OAuth 配置并标记为已授权，无法先验证新凭据，也无法在重授权失败时可靠保留旧凭据。

### 问题原因

授权流程只有一组可变状态和一份当前令牌，没有区分授权流程状态、运行健康状态与 staged/active 凭据版本；state 也在 Google 兑换结束后才清理，存在并发重复消费窗口。失败分支还会保存 Google 原始错误文本，带来敏感响应泄露风险。

### 修改内容

- 新增授权请求参数 `force_reauthorize` 和 `reason`。健康账号默认拒绝重新授权，强制重授权必须记录原因。
- 同一 OAuth App 存在未过期授权 state 时返回 409，防止并行发起多条授权流程。
- 仅首次授权、已撤销账号或显式强制重授权时添加 `prompt=consent`，减少不必要的新 refresh token 签发。
- callback 在调用 Google token endpoint 前原子消费一次性 state；重复 callback 立即拒绝。
- Google 必须返回新的 refresh token 才能创建 `staged` 凭据；缺失时返回 422，不复用旧 token，也不覆盖当前 active 凭据。
- 新 client secret 和 refresh token 只写入加密的 `oauth_credentials` 版本；不再持久化 access token、token type 或 access token 到期时间。
- callback 只进入 `validation_pending`，不直接切换为可运行的 `authorized/healthy`。后续由凭据验证和 ACK 流程激活。
- OAuth 审计事件只记录固定事件类型、失败分类、HTTP 状态和流程状态，不保存 code、callback URL、token payload 或 Google 原始响应正文。
- 兼容旧运行配置时只检查真实的旧版 refresh token 字段，避免把受管凭据版本误当作仍有明文令牌。

### 影响范围

影响 OAuth 授权链接、callback 导入、授权状态展示和旧版运行配置兼容路径。callback 后的新凭据在后续验证任务完成前不可用于普通拉取；现有 active 凭据在重授权失败时保持不变。前端或运维调用健康账号重授权接口时，需要显式提交强制标志和原因。

### 修改结果

- OAuth 状态机与路由专项测试：`48 passed`
- 后端全量测试：`94 passed`
- 已验证一次性 state 在网络请求前被消费、重复 callback 被拒绝、失败重授权不覆盖 active 凭据，且数据库与审计事件不保存 access token 或原始敏感响应。

## 2026-07-31：OAuth 失败只有原始异常，无法判断是否可重试或需要重新授权

### 修改问题

collector 刷新 access token 时直接调用 `raise_for_status()`，后端授权码兑换也只记录笼统的交换失败。`invalid_grant`、`invalid_client`、429、Google 5xx、网络超时和响应契约错误无法被稳定区分，调度器无法据此决定重试、熔断或要求人工重新授权。

### 问题原因

OAuth 网络调用缺少统一错误模型，业务层依赖 HTTP 库异常和 Google 原始响应。异常字符串可能包含请求上下文，既不适合作为控制信号，也存在把 token 请求细节带入日志的风险。

### 修改内容

- 后端和 collector 新增一致的 `OAuthFailure`，仅暴露 `failure_class`、`retryable`、`http_status`、受控 `error_subtype` 和固定 `public_message`。
- 授权码 `invalid_grant` 分类为 `oauth_code_invalid`；refresh grant 的 `invalid_grant` 分类为 `oauth_refresh_revoked`，带 `invalid_rapt` 时分类为 `oauth_session_expired`。
- `invalid_client` 分类为 `oauth_client_invalid`；429 和 5xx 分别分类为可重试的 `oauth_rate_limited` 与 `oauth_provider_unavailable`。
- 网络超时、其他传输失败和响应契约错误分别使用固定类别，不保存原始异常或响应正文。
- collector 不再调用 `raise_for_status()`，而是先安全解析固定错误字段，再抛出结构化异常；传输异常不会作为 cause/context 挂到业务异常上。
- 后端 callback 使用同一分类写入脱敏 OAuth 审计事件，因此授权码失效不会再与 refresh token 撤销混淆。

### 影响范围

影响后端 OAuth callback 错误记录和 collector access token 刷新异常类型。成功路径和 token 请求参数保持不变；调用方后续可以直接使用 `failure_class` 与 `retryable` 实现熔断和恢复策略。

### 修改结果

- 后端错误分类与 callback 专项测试：`20 passed`
- collector OAuth 刷新专项测试：`8 passed`
- 后端全量测试：`103 passed`
- collector 全量测试：`79 passed`
- 已验证分类覆盖授权码、refresh token、会话、客户端、限流、服务端错误、超时和响应契约错误，异常文本、repr、cause/context 均不包含 refresh token。

## 2026-07-31：新 refresh token 未经账号代理和 Google Network 验证就会直接生效

### 修改问题

旧授权 callback 在取得 token 后直接把账号视为已授权，未验证 refresh grant、实际 scope、Google Ad Manager Network code 和 Network 时区，也没有凭据版本 ACK。错误账号、错误 OAuth Client 或不完整 scope 可能进入运行链路，迟到 callback 也可能覆盖更新版本。

### 问题原因

控制面与 collector 之间缺少 staged 凭据验证协议；运行配置只能读取旧明文 token，任务系统也没有 `oauth_credential_validate` 和 `oauth_health_check` 两类受控任务的认领与终态规则。

### 修改内容

- callback 为已有 collector 实例创建 `oauth_credential_validate` 任务，新凭据保持 `staged/validation_pending`。
- 验证任务运行配置只解密当前 `pending_credential_version`，携带版本、HMAC 指纹和授权 scope，并继续使用账号绑定代理。
- collector 按固定顺序执行 refresh grant、token type/scope 校验、同代理 NetworkService `getCurrentNetwork()`、network code 和 IANA 时区校验。
- access token 仅保存在验证调用栈内存中，不进入 ACK、任务 message、事件或数据库。
- 新增 `POST /api/v1/collector/oauth/credential-ack`。ACK 必须匹配认证实例、验证任务、账号、当前 pending 版本、完整 HMAC 指纹和 network code。
- ACK 激活事务先将旧 active 版本设为 retired，再将 staged 版本设为 active，同步真实 Network 时区，并把 OAuth runtime 置为 degraded。
- 激活后创建一个已成熟日期的 `oauth_health_check` 任务；只有后续健康任务成功才允许恢复普通调度。
- 验证任务和健康任务优先于普通小时任务认领，并使用对应 staged/active 版本通过统一 fetch policy。
- 验证失败时只拒绝 staged 版本，原 active 版本不受影响；OAuth 任务 message 在 collector 和控制面两侧均使用固定错误码白名单，任意输入不会写入日志。
- 旧版本、错误指纹、错误 Network 或重复 ACK 统一返回 `409 STALE_CREDENTIAL_ACK`。

### 影响范围

影响 OAuth callback 后续任务、collector 运行配置、任务认领、Google NetworkService 调用、验证失败回报和凭据激活事务。普通报表成功路径尚未切换到受管 active 凭据，该切换在下一项 direct collector 改造中完成。

### 修改结果

- 后端全量测试：`106 passed`
- collector 全量测试：`83 passed`
- 已验证 staged 验证顺序、SOCKS5 代理传递、错误 scope/network 拒绝、指纹与版本防重放、时区同步、旧版本退休、健康任务创建以及任务错误脱敏。

## 2026-07-31：控制面和节点公网入口会同时触发拉取，形成双路径和重复资源消耗

### 修改问题

手工小时拉取和自动小时 schedule 先调用节点 `/ke/fetch.php` 创建节点 fetch run，再创建控制面任务并启动 direct collector。两条路径可能同时请求 Google、重复写节点数据库，并要求每个节点持续维护 `report_base_url`、account key 和 report token。

### 问题原因

系统从节点公网拉取架构迁移到控制面任务架构时保留了旧触发步骤，控制面任务没有成为唯一执行信号；节点 `/public/fetch.php` 和 PHP 代理入口也默认保持可用。

### 修改内容

- 新增默认开启的 `direct_collector_only` 配置，后端环境变量为 `ADX_COLLECTOR_DIRECT_COLLECTOR_ONLY`，节点入口环境变量为 `ADX_DIRECT_COLLECTOR_ONLY`。
- 默认手工及自动小时触发只生成控制面 request ID、创建或复用 `CollectorSyncTask` 并启动该账号 runtime，不再调用 `/ke/fetch.php`。
- direct-only 路径不再要求节点 `report_base_url`、`report_account_key` 或 `report_token`，账号代理和受管 active OAuth 凭据成为运行依赖。
- 普通 runtime config 从控制面的加密 active 凭据解密 client secret 和 refresh token，并携带凭据版本、指纹及 scope；不再依赖节点 MySQL token。
- 节点 `/public/fetch.php` 和 PHP `fetch.php` 在 direct-only 开启或未配置时返回 `409 FETCH_PATH_DISABLED`。
- 保留显式关闭开关后的旧路径，仅作为紧急兼容回退，并用独立测试覆盖；默认测试证明不会调用远程 fetch。
- `ADX_DIRECT_COLLECTOR_ONLY=true` 已加入 VPS 环境变量示例。

### 影响范围

影响手工小时任务、自动小时 schedule、collector runtime config、节点公网 fetch API 和 PHP 兼容入口。节点日报查询接口不受影响。发布后默认不会再创建节点侧 fetch run，数据由控制面任务经 direct collector 拉取并直接提交中台。

### 修改结果

- 后端全量测试：`107 passed`
- collector 全量测试：`84 passed`
- 虚拟端到端流程：`1 passed`
- 手工触发与 scheduler 专项测试：`48 passed`
- 节点 API 专项测试：`11 passed`
- 本机未安装 PHP CLI，无法运行 `php -l`；PHP 改动仅为入口顶部的环境开关与固定 JSON 响应，仍需在发布检查中使用服务器 PHP CLI 做语法验证。

## 2026-07-31：OAuth 失效后缺少受控复验、熔断和恢复，造成失败任务反复堆积

### 修改问题

普通拉取任务遇到 `invalid_grant` 后只会记录一次失败，调度器仍可能继续创建后续任务；运维重新授权后也缺少按原策略恢复 schedule 和补扫数据缺口的统一流程。原始异常文本还可能通过任务状态接口进入控制面，既无法稳定驱动状态机，也存在敏感上下文落库风险。

### 问题原因

控制面此前没有区分“首次发现 OAuth 异常”和“专用健康检查确认凭据已撤销”，也没有保存灰度、小时、日报与 schedule 的恢复快照。collector 回报仅有自由文本 message，无法让控制面依据固定错误分类执行降级、复验、熔断和恢复。

### 修改内容

- 任务状态回报新增受控 `failure_class`，collector 对 OAuth 异常只回报结构化分类，其他异常统一为 `collector_task_failed`，不再上传原始异常字符串。
- 普通任务首次遇到 `oauth_refresh_revoked` 时将 OAuth runtime 标记为 `degraded`，累计失败次数，并只创建一个活动中的 `oauth_health_check` 任务，避免并发复验。
- 专用健康检查再次确认 refresh token 撤销后打开熔断：保存当前灰度、小时、日报与 schedule 恢复快照，设置 `invalid_grant` 停拉，禁用拉取策略和 schedule，并阻塞尚未开始的待执行任务。
- 熔断操作具备幂等性：重复失败不会覆盖首次恢复快照，也不会重复写入 `oauth_circuit_opened` 审计事件。
- `oauth_health_check` batch 使用专用健康检查策略，允许 degraded 状态下提交最小验证数据，而不会放开普通拉取入口。
- 健康检查成功后仅自动清理由系统熔断写入的 `invalid_grant`，不会清除人工停拉原因；随后按快照恢复灰度和拉取开关。
- 恢复的小时 schedule 统一调整为每 4 小时一次，并根据账号错峰计算下次运行时间；当没有活动任务时，为账号当前本地日期创建一个小时缺口任务，同时记录恢复和缺口扫描审计事件。

### 影响范围

影响 collector 任务失败回报、控制面任务终态处理、OAuth 健康检查 batch、账号策略和小时 schedule 的熔断与恢复。人工停拉优先级保持不变；普通报表成功路径和只读历史数据接口不受影响。上线前仍需通过迁移工具为存量账号建立受管凭据、策略和恢复状态，不能直接依赖旧明文 token 配置。

### 修改结果

- 后端全量测试：`110 passed`
- collector 全量测试：`85 passed`
- 已验证首次失败只创建一个健康检查、二次确认后幂等熔断、待执行任务阻塞、策略快照恢复、人工停拉不被误清除、4 小时错峰 schedule 恢复和最新小时缺口任务创建。
- `git diff --check` 通过；测试仅存在 pytest-asyncio 未来默认作用域变更的弃用警告，与本次逻辑无关。

## 2026-07-31：存量 OAuth 秘密和灰度策略无法安全迁移到统一控制面

### 修改问题

新版本已经使用加密、版本化 OAuth 凭据和数据库账号策略，但存量账号仍保留明文 Client secret、refresh token、access token，以及代码常量中的灰度和停拉集合。直接切换会导致账号缺少 active 凭据或 policy；逐账号手工搬运则容易出现部分成功、重复版本、秘密输出和灰度/停拉冲突。

### 问题原因

Alembic 迁移只负责表结构，不能读取运行环境密钥并搬运秘密；项目此前也没有事务型一次性迁移命令。授权 callback 仍直接读取 legacy `oauth_app.client_secret`，因此单纯清空旧字段还会导致后续重新授权无法兑换 token。

### 修改内容

- 新增 `python -m app.scripts.migrate_oauth_credentials`，在一个数据库事务中逐账号加密 Client secret 和 refresh token、生成 HMAC 指纹、创建 version 1 active 凭据并立即解密回读校验。
- 只有全部待迁移账号成功后才清空 legacy Client secret、refresh token、access token、过期时间和 token type；任一加密或回读失败会回滚全部凭据、任务、schedule 和旧字段变更。
- 迁移命令幂等：已有 active 凭据只做解密可读性检查，不创建新版本或重复健康任务；输出只含 `account_id`、版本、指纹和固定状态。
- 完整存量凭据迁移后设置 `runtime_status=unknown`，暂停该账号 schedule，并创建唯一 `oauth_health_check` 任务；缺少完整 legacy 凭据的配置标记为 `migration_required`，等待完成授权。
- 正式计划原文写“创建 credential validation task”，但现有状态机规定 `oauth_credential_validate` 只能验证 staged 凭据。存量凭据按设计直接迁为 active，因此本实现使用 active 凭据专用的 `oauth_health_check`，避免伪造 staged/ACK 状态，同时保持验证前不恢复 schedule 的目标。
- 新增 `python -m app.scripts.migrate_collector_account_policies`，从原灰度、invalid-grant、人工停拉集合和 schedule 生成数据库 policy；灰度与任一停拉集合冲突，或两类停拉原因互相冲突时，在写入前整体终止并返回固定冲突码。
- policy 迁移对已有记录不覆盖，重复执行不会新增或重置人工修改；报告只包含 `account_id` 和固定 policy 状态。
- OAuth callback 在 legacy Client secret 已清空时，从当前 active 加密凭据解密 Client secret 完成 authorization-code 兑换；新 staged 凭据 ACK 激活后同步清空全部 legacy token 字段。

### 影响范围

新增两个本地一次性迁移命令，并影响 OAuth callback 的 Client secret 读取和凭据 ACK 后的旧字段清理。迁移命令默认连接当前控制面数据库，实际运行前必须停止 scheduler、备份数据库并注入两把独立密钥。命令和测试不访问生产环境，也不会自动部署或修改线上数据库。

### 修改结果

- 迁移与 OAuth 状态机专项测试：`21 passed`
- 后端全量测试：`115 passed`
- `git diff --check` 通过。
- 已验证凭据迁移幂等、任一账号失败整体回滚、legacy 字段清理、schedule 暂停、唯一健康任务、policy 冲突零写入、报告脱敏，以及迁移后重新授权继续使用 active 加密 Client secret。

## 2026-07-31：OAuth 恢复后按固定日期批量重跑，缺少真实缺口判断和全局串行约束

### 修改问题

账号重新授权并恢复健康后，旧逻辑会直接为当前本地日期创建小时任务；自动回补也容易按固定“最近几天”创建多项任务。它没有统一使用完整日 coverage、当前 UTC 水位和成功权威日报任务判断真实缺口，也无法保证不同账号的恢复任务不会在同一轮并发启动。失败缺口若被反复入队，还会重新形成 CPU 和 Google API 资源风暴。

### 问题原因

健康恢复函数、targeted recent 和自动日报各自创建任务，缺少单一 recovery gap 扫描器与可识别的恢复任务类型。scheduler 也没有全局恢复并发限制；新建任务若只 flush 就启动独立 collector 子进程，子进程还可能在 SQLite 提交前查询不到任务。

### 修改内容

- 健康恢复只写入 `oauth_gap_scan_requested` 审计事件，不再直接为当前日期创建小时任务。
- 新增 OAuth 恢复缺口扫描器，仅处理已产生 gap-scan 事件、当前 `healthy`、仍在灰度且未停拉的账号，避免对全部灰度账号无差别扫描。
- 对已结束业务日，优先读取 `account_report_day_statuses` 的完整日 coverage，并以现有小时 coverage 作为兼容回退；完整日期不创建小时任务。
- 对当前业务日，以账号最新 `report_time_utc` 与“当前 UTC 前一小时”水位比较，只在实际落后时创建小时恢复缺口。
- 权威日报只对已经满足账号 Network 时区成熟等待条件、且不存在成功 `report_fetch` 任务的日期生成缺口；小时缺口始终排在权威日报缺口之前。
- 恢复任务统一使用 `run_reason=oauth_recovery` 和确定性 request ID。相同账号、任务类型和日期只自动入队一次；曾经失败的恢复任务不会被 scheduler 自动无限重试，需通过运维判断原因后显式处理。
- scheduler 全局最多保留一个 `pending/in_progress/blocked` 的 OAuth recovery 任务。当前任务终态后，下一轮才会选择下一个真实缺口，因此同账号和不同账号均不会在一轮内无界启动。
- 创建恢复任务后先提交 SQLite 事务，再启动独立 collector runtime，确保子进程从新会话能读取 pending 任务；runtime 启动异常时将该恢复任务标记为失败。

### 影响范围

影响 OAuth 健康恢复后的缺口任务创建和 scheduler 每轮处理。正常 4 小时实时 schedule、既有自动权威日报和手工补数接口保持原有入口；新的全局并发限制只约束 `run_reason=oauth_recovery` 的受控恢复队列。默认扫描窗口为账号当前业务日及此前 3 天，但不会重跑 coverage、水位和权威任务均已完整的日期。

### 修改结果

- 恢复、熔断和 scheduler 专项测试：`19 passed`
- 虚拟端到端流程：`1 passed`
- 后端全量测试：`119 passed`
- `git diff --check` 通过。
- 已验证完整日期零任务、小时缺口优先、成熟权威日报其次、当前 UTC 水位判断、失败缺口不自动重试、全局只启动一个恢复账号，以及 runtime 启动前任务对独立数据库会话可见。

## 2026-07-31：运维前端无法区分 OAuth 授权流程与运行健康状态，容易重复授权

### 修改问题

原 OAuth 页面只显示 `authorization_status` 和 refresh token 是否存在，所有账号都提供同一个“Generate URL”按钮。运维无法判断凭据正在验证、正在健康检查、已经撤销还是仍然健康，也看不到当前版本、指纹、失败分类和下一步动作。健康账号可在没有确认和原因的情况下重复生成授权链接，增加新 refresh token 被频繁签发和旧 token 被 Google 淘汰的风险。

### 问题原因

后端 OAuth 列表响应尚未包含新状态机字段和凭据指纹；前端类型、API 请求和操作组件仍沿用旧单状态模型，也没有传递 `force_reauthorize` 与 `reason`。

### 修改内容

- OAuth 列表 API 新增 flow status、runtime status、active/pending 版本、HMAC 指纹、failure class/count、last verified、revoked time、publishing status 和 next action。
- 列表服务只从 active 或 staged 凭据读取 HMAC 指纹，不返回 Client secret、refresh token、密文或 access token；创建接口依旧由响应模型提供空指纹默认值。
- 前端 OAuth 类型和授权 API 支持 `force_reauthorize` 与 `reason`。
- 现有紧凑表格改为展示 flow、runtime、版本、12 位短指纹、失败分类与次数、最近验证时间和下一步动作；不新增单独营销式页面。
- `validation_pending` 和 `requested` 禁止重复生成授权链接；`degraded` 显示健康检查进行中；`policy_blocked` 要求先处理策略；`revoked` 提供恢复授权动作。
- `healthy` 账号重授权前必须二次确认并填写非空原因，随后才以强制重授权请求调用后端；取消确认或缺少原因时不请求 API。
- 新增服务端静态渲染组件测试，验证各状态的按钮文本、禁用状态、短指纹及页面不渲染秘密字段。

### 影响范围

影响 `GET /api/v1/operator/oauth-apps` 的响应字段和运维前端 OAuth 表格及授权操作。新增字段保持向后兼容，原字段未删除；OAuth callback、collector runtime 和数据拉取接口不受本次 UI 变更影响。

### 修改结果

- 后端全量测试：`120 passed`
- 前端全量测试：`20 passed`
- 前端 TypeScript 与 Vite 生产构建成功。
- `git diff --check` 通过，构建产物未进入 Git 变更。
- 已验证健康重授权限制、revoked 恢复入口、validation pending 防重复、degraded 健康提示，以及 API 与页面均不包含 OAuth 秘密。

## 2026-07-31：缺少统一 OAuth 健康指标，无法快速识别版本漂移、撤销违规和数据延迟

### 修改问题

OAuth 状态分散在账号配置、凭据版本、任务和报表表中。运维只能逐账号查询，无法一次得到各 runtime 状态数量、失败分类累计次数、凭据版本不一致、revoked 后仍创建任务的违规，以及小时和权威日报延迟，也难以为后续告警建立稳定数据源。

### 问题原因

控制面此前没有结构化健康摘要接口；现有列表接口面向账号配置展示，不负责跨表聚合。若在摘要请求中临时调用 Google，又会增加外部请求、代理和限流风险，因此必须仅依赖本地数据库事实。

### 修改内容

- 新增 `GET /api/v1/operator/oauth/health-summary`，返回生成时间、runtime 状态账号数、按 failure class 累计失败次数、凭据版本不一致总数、revoked 后任务违规总数和逐账号延迟。
- runtime 和 failure 指标使用数据库分组聚合；失败指标累加账号的 `failure_count`，不读取或返回原始异常文本。
- 凭据版本一致性同时校验 `active_credential_version -> active` 和 `pending_credential_version -> staged` 的实际版本行。
- revoked 违规只统计账号 `revoked_at` 之后创建的 `report_fetch` 与 `report_fetch_hourly` 任务，用于识别停拉策略被绕过的 P0 风险。
- 小时水位延迟定义为当前 UTC 与账号最新 `report_time_utc` 的小时差；无小时数据时返回 `null`，不伪造水位。
- 权威日报延迟严格按账号 Network 时区计算最新成熟业务日；若有历史成功任务，从第一个缺失业务日的 `authoritative_daily_ready_at` 起算，已经追平则为 0。
- 摘要实现只查询本地控制面表，不访问 Google、节点公网入口或代理，也不返回 token、Client secret、密文、实例 token 或代理密码。
- 首次撤销告警只写一次、后续失败累加次数的行为继续复用任务 9 已实现并测试的幂等熔断事件；本接口只读取和汇总，不重复写告警事件。

### 影响范围

新增只读 operator API 和对应响应 schema，不修改 OAuth 状态、任务或报表数据。现有 API 路径与响应保持不变；后续监控系统可直接采集该摘要建立 P0/P1 告警。

### 修改结果

- 健康摘要 API 专项测试：`1 passed`
- 后端全量测试：`121 passed`
- `git diff --check` 通过。
- 已验证 runtime/failure 聚合、版本漂移、revoked 任务违规、小时水位与权威日报成熟延迟，并通过测试强制保证摘要请求不访问 Google 且响应无秘密。
