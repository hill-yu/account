# 问题修改记录

本文件记录本项目每次代码修改对应的问题、原因、影响范围和验证结果。任何运行代码、测试、配置模板或迁移脚本的修改，都必须在同一提交中更新本文件。

## 2026-07-31：生产实现与旧测试漂移

### 修改问题

从生产服务器同步源码后，后端测试有 10 项失败，collector 测试有 17 项失败。失败测试仍按旧版远端 snapshot 日报、旧 Ad Manager 报表列、旧灰度名单和旧数据表集合进行断言，无法作为 OAuth 凭据整改的可靠回归基线。

### 问题原因

生产环境曾直接修改运行代码，但对应测试没有同步更新。生产代码已经具备以下行为：

- 中台日报接口读取本地 `account_daily_reports` 和 `site_daily_reports` 权威入库结果。
- 权威日报查询使用 `DATE` 维度和 `PUBLISHER` 时区。
- 小时报表包含国家、广告单元和 `AD_EXCHANGE_TOTAL_REQUESTS`。
- 日报与小时数据模型均包含 `requests`。
- 小时覆盖结果包含日报与小时聚合值比对字段。
- 自动日报按当前生产灰度名单和停拉名单执行，普通日报聚合行不代表权威日报任务已经成功。
- 元数据已经包含 `fetch_schedules` 和 `account_report_day_statuses`。

### 修改内容

- 更新 collector SOAP 查询、CSV 输入和解析结果测试，覆盖日报 `PUBLISHER` 口径及小时新增维度。
- 更新日报服务和 fetcher 测试，补齐 `requests`、小时来源时区及覆盖式小时 batch 参数。
- 更新中台日报接口测试，写入本地权威日报并禁止测试期间调用节点 snapshot。
- 更新自动日报与 targeted-recent 测试账号，匹配当前生产灰度和停拉集合。
- 更新小时覆盖和模型元数据断言。

### 影响范围

仅修改测试与本记录文件，不修改任何生产运行代码、数据库结构或部署配置。

### 修改结果

- 前端：`16 passed`
- 后端：`66 passed`
- collector：`73 passed`

三套现有测试恢复全绿，可作为后续 OAuth 凭据统一控制面整改的基线。
