# 问题记录

## 2026-08-03：权威日报首次快照被永久当作完整数据

`coeurdazur` 的 2026-08-02 权威日报首次快照不完整，但下游将其永久去重。根因是旧调度过早尝试且运行代码未写入、返回 `is_finalized`。整改采用延后尝试、最终化状态和 API 完整标志；小时链路继续禁止覆盖权威日报。详细事故时间线和历史整改证据保留在原运维记录中。

## 2026-08-06：源时区跨日后未回查上一业务日，造成北京时间小时数据缺口

### 现象与根因

小时源固定为 `America/Los_Angeles`。scheduler 在源时区跨日后立即把 `report_date` 切换到新日期，不再回查上一源业务日。如果上一业务日最后一小时尚未被 Google 返回，该小时会永久缺失。PDT 下源小时 23 对应次日北京时间 14:00。

生产只读审计确认该问题具有共性：2026-08-09 的北京时间 14:00 同时在 `coeurdazur`、`cpatobe`、`linkzclub`、`onlyfungogo`、`reboroots`、`skouje` 缺失；2026-08-10 同一小时在其中五个仍缺失。对应源日期均为 `00–22`，缺少源小时 23。日期结束后人工拉取的 `tqchq.com` 可得到完整 `00–23`，验证了根因。

低量账号不足 24 个非空小时不等于存在缺口，因为 Google 可能不返回真实零数据小时；不得据此批量重拉。

### 统一代码整改

1. 通过 `ADX_COLLECTOR_CROSS_DAY_FINALIZE_ACCOUNT_KEYS` 明确控制灰度账号，默认关闭。
2. 在 Pacific 新业务日 01:00–02:59 的正常小时调度周期，若上一源日期尚无成功最终刷新，则用本周期重拉上一源日期，不额外增加任务量；下一周期恢复当前日期。
3. 任务标记 `run_reason=cross_day_finalize`，使用确定性请求 ID；成功后不重复，活跃任务复用，失败最多重试一次；两次失败后写入唯一的 `cross_day_finalize_exhausted` 阻塞记录，避免缺口静默。
4. 最终刷新只替换该账号、该 Pacific 源日期的小时分区，不写入或重建账户/站点权威日报。
5. 历史缺口不自动回补，仍须逐账号、逐日期备份和受控处理。
6. 本功能只在 `direct_collector_only=true` 时生效；非 direct 模式继续执行原当前日任务，避免旧远端协议在调用前无法落库而突破幂等和重试上限。

### 验证与发布边界

TDD 覆盖首次回查、成功幂等、活跃任务复用、两次失败上限、窗口外和开关外不回查、DST、任务标记落库，以及小时批次不覆盖权威日报。代码必须通过独立审阅和 Git 提交后，先仅对 `coeurdazur` 灰度并观察两个 Pacific 跨日周期；未验证前不得扩大节点范围。

2026-08-11 后续经用户明确授权扩大灰度。扩大范围仅包含 OAuth 健康且已授权、代理有效、每小时 schedule 已启用且使用 Pacific 调度时区的节点：`coeurdazur,cpatobe,dddfdc,ddgjcj,linkzclub,onlyfungogo,reboroots,skouje,tqchq,uragnv,zilote`。`stones` 的计划间隔为 4 小时，可能错过两小时最终刷新窗口，因此未纳入；其他停用或授权不满足门禁的节点也未纳入。

### 回滚

清空灰度账号配置即可立即停用新行为；代码回滚不涉及数据库迁移。必要时定向取消该账号尚未执行的 `cross_day_finalize` 任务，不做整库恢复。

## 2026-08-12：linkzclub OAuth 实际失效但状态仍健康，导致任务风暴与数据中断

### 事故现象与根因

`linkzclub.com` 的小时事实水位停在北京时间 2026-08-09 23:00，权威日报停在账户业务日 2026-08-08。账号、实例、schedule、代理及数据库 OAuth 状态表面均正常，但从北京时间 2026-08-10 02:06 左右开始，小时与日报任务同时持续失败。隔离检查确认代理实际出口仍与期望出口一致；使用活动 v2 托管凭据分别经代理和直连 Google token endpoint，均返回 `Token has been expired or revoked.`。根因是 refresh token 已撤销或过期，而数据库仍错误保留 `authorized + healthy`。

### 暴露的问题

1. collector 把底层异常统一回传为 `collector_task_failed`，OAuth、代理、SOAP、超时等原因无法从任务日志区分。
2. OAuth `runtime_status` 未因真实 token 刷新失败及时转为 revoked/degraded，schedule 继续创建必败任务。
3. 权威日报失败后约每 30 秒重复创建新任务，单日累计超过千条，缺少按账号/报告日幂等和退避。
4. 代理表的 `active` 与健康检查时间可能长期陈旧，排障不能只信数据库状态。
5. 健康检查任务实际 `succeeded`、OAuth 已恢复 `healthy`，但日志 message 却为 `oauth_validation_failed`，终态和日志语义矛盾。

### 本次恢复结果

仅对 account 33 / OAuth App 32 执行强制重授权。callback 兑换生成 staged v3；凭据验证任务成功后 v3 激活、v2 退休；OAuth 健康检查成功，最终恢复 `authorized + healthy`。代理、schedule 和其他账号未修改。兑换前在线 SQLite 备份为 `/srv/adx-account-isolated-collector/backups/20260812T065900Z-pre-linkzclub-callback-exchange`，`quick_check=ok`。

### 后续代码整改

需要另立 TDD 任务：保留安全脱敏后的失败分类；token endpoint 明确返回 revoked/expired 时立即打开 OAuth 熔断；权威日报失败按账号+报告日幂等并指数退避；健康检查成功日志不得写失败文案；健康汇总应暴露代理检查陈旧度。整改前不得以数据库 `healthy` 单独证明 OAuth 可用。
- 2026-08-12 数据恢复：OAuth v3 恢复后，仅对 `linkzclub.com`（account 33）串行补拉。小时任务 `25346—25349` 全部成功；8 月 8—10 日各 24 小时，8 月 11 日为 Google 当前返回的 12 个非空小时快照。权威日报任务 `25350/25352/25354` 分别恢复 8 月 9—11 日并成功。补拉前备份 `20260812T072216Z-pre-linkzclub-backfill` 校验正常，小时补拉未覆盖既有权威日报。
- 2026-08-12 操作问题：首次日报补拉在客户端等待超时后仍于服务器后台继续，直接重跑脚本触发相同 `external_request_id` 唯一约束；数据库安全拒绝重复插入，未造成重复业务任务。以后必须先查既有请求号/任务终态再决定是否重试，禁止把客户端超时当成服务器未执行。

## 2026-08-13：交接任务未建立真实 worktree，且缺少固定生产执行基线

### 背景、现象与影响

此前创建的“ADX 中台维护交接（主目录）”实际 cwd 为 `D:/code/adx-mid-platform`，不是新的 Git worktree。该主目录存在大量未提交、未跟踪和演进中的文档；交接摘要只列原则，没有固定可信提交、生产服务器和路径核验方法、当前 schema/API 校准方式及完整节点接入顺序。后续任务从本机历史误选旧服务器，并连续出现 SSH 公钥状态误判和多层 shell 引号错误。失败均发生在节点生产写入前，没有创建目标节点或修改其他生产资源，但造成重复操作和时间损失。

### 根因与解决方案

根因是交接环境和交接内容同时不完整：将“新对话”错误等同于“新工作树”，并依赖脏主目录中的未版本化资料；执行方又没有贯彻“现场只读核验后再操作”。已从 `origin/master@b157a63000763c8ef7b9967fc3d74cfe4822a4e7` 创建干净 worktree/分支，并新增 `docs/production-ops-handoff-runbook.md`，把生产现状、现场门禁、精确接入步骤、已验证命令模式、禁止事项和定向回滚固定为新的交接入口。

### 验证、状态与防再犯

新 worktree 创建后 Git 状态干净，未从主目录复制未提交文件，未执行生产连接或写入。当前状态：交接环境问题已解决；生产事实仍必须在每次任务开始时现场复核。以后不得用主目录 cwd 的新对话冒充 worktree，不得仅靠摘要或历史命令执行生产操作；新任务必须报告 worktree 绝对路径、分支、HEAD 和 clean 状态后才能进入生产只读门禁。

### 追加：Runbook 复审修订补丁锚点不匹配

独立审阅提出修订后，首次 `apply_patch` 根据记忆使用了缺少“并”字的长句锚点，补丁在校验阶段被完整拒绝，没有产生部分文件写入或生产影响。根因是未先读取目标段落精确文本。正确替代是先用 `rg -n -C` 定位实际段落，再使用短且唯一的精确锚点实施最小补丁，并在完成后运行 `git diff --check`。该本地失误已解决；以后长文档补丁禁止依靠记忆重建锚点。

### 追加：PowerShell 分号未阻断失败的提交前检查

首次提交时将 `git diff --cached --check`、`git commit` 和 `git push` 以分号串联。检查正确发现 Runbook 头部三行尾随空格并返回非零，但 PowerShell 分号不会因前一原生命令失败自动停止，后续提交和推送仍执行，形成提交 `ed32a3e`。影响仅为远端分支短暂包含三行 Markdown 尾随空格；没有生产部署、敏感信息或功能代码影响。正确做法是逐条执行，或每个原生命令后检查 `$LASTEXITCODE` 并 `throw`；提交前检查非零必须阻断 commit 和 push。已使用后续提交修正空格并保留完整历史，禁止通过强推隐藏失误。

## 2026-08-13：babyboutiq.com 单节点接入耗时异常复盘

### 背景与现象

本次复盘涉及两段不同事件，必须分开理解：

1. 2026-08-12 的实际生产接入：`babyboutiq.com`（network code `23349156277`）完成基础资源、OAuth、健康检查、受控小时拉取以及后续灰度/schedule。证据来自主目录当时追加但尚未纳入 `b157a63` 的维护台账和问题记录，资源和任务 ID 见下文。
2. 随后的“ADX 中台维护交接（主目录）”任务再次尝试为同名节点执行接入，但受阻于 SSH 和交接基线问题。该任务从 2026-08-12 约 15:58 至 16:15 的可读任务记录持续约 17 分钟；若从其更早的节点申请轮次计算，跨度更长，但现有分页记录不足以给出完整起点。该重复尝试最终没有写生产、没有创建 OAuth state 或 schedule。

因此不能把重复交接任务的 SSH 错误混作第一次生产接入中的业务步骤，也不能把两段持续时间简单相加为一个精确“接入耗时”。

### 必要耗时与可避免耗时

必要门禁包括：确认账户报告时区和币种、生产唯一性和 schema、专属代理实际出口、SQLite online backup 与 `quick_check`、OAuth state/callback、credential validation、health check、小时和日报独立验收。这些步骤符合生产安全要求，不能省略。

现有证据可以确认以下可避免步骤增加了返工，但由于原操作没有逐阶段计时，无法量化其耗时占比，也无法与 Google OAuth 页面等待时间做精确比例比较：

1. 初始申请缺少账户时区和币种，执行到写库前才发现必填信息不完整。
2. 唯一性查询沿用旧字段 `collector_instances.instance_key`，未先读取生产 `PRAGMA table_info`。
3. 从 Windows 本机用通用 Python 测试生产 SOCKS5 代理，环境和来源均不能代表生产 collector。
4. 将中文本地 OAuth JSON 绝对路径嵌入 PowerShell/SSH/Python 多层脚本，路径编码损坏。
5. 猜测 Operator Token 所在 `.env` 和变量名，未先读取 systemd `EnvironmentFile` 及运行代码配置字段。
6. credential validation 后仍保持 policy `onboarding`，未识别 health check 要求 `lifecycle_status=active`，造成 409 和 Pending。
7. 开启 schedule 前没有预演 OAuth 恢复缺口扫描；首次周期创建了用户精确补拉范围外的日期任务，必须紧急禁用目标 schedule 并核对已认领任务。
8. 后续重复交接任务实际运行在脏主目录而非独立 worktree，交接摘要没有固定 Git、服务器、路径、schema/API 和完整状态机。
9. 后续重复交接任务从历史记录误选旧服务器，并在 SSH 公钥已生成、已安装、是否被服务接受之间反复误判；多层 shell 引号、剪贴板依赖和清理命令又产生无效尝试。这些问题有该任务消息时间和失败输出支持，但不属于第一次生产资源创建阶段。

### 可追溯历史证据

以下均为 2026-08-12 操作完成时的历史快照，不代表 2026-08-13 当前状态；再次生产操作前必须现场核验：

- 基础资源：account 39、instance 39、OAuth App 38、staged credential 50、proxy 39、policy 39；备份 `20260812T134359Z-pre-babyboutiq-onboarding`，备份和生产库 `quick_check=ok`。
- 授权链接：备份 `20260812T135041Z-pre-babyboutiq-auth-url`；首次脚本在 HTTP 前以 `operator_token_not_found` 停止，纠正为从 systemd 实际环境经 `get_settings().operator_api_token` 读取后成功生成 OAuth App 38 链接。
- OAuth：凭据验证任务 `25419` succeeded；健康任务 `25420` 因 policy lifecycle 为 `onboarding` 两次收到 409 `FETCH_POLICY_EXCLUDED`，policy 39 改为 `active`、policy_version 2 且四开关仍关闭后，复用 `25420` succeeded，OAuth v1 为 `authorized + healthy`。
- 受控小时拉取：备份 `20260812T141449Z-pre-babyboutiq-recent-data`；Pacific 8 月 9、10 日任务 `25431/25432` succeeded，但 batch `9666/9670` 均为 0 行，因此当时只能证明链路完成，不能宣称有报表数据。
- 灰度和 schedule：备份 `20260812T143018Z-pre-babyboutiq-gray-schedule`；schedule 35 首轮额外创建任务 `25434—25438`。止损后这些任务最终均 succeeded、无 Pending/In Progress，schedule 35 恢复为每小时 `America/Los_Angeles`，历史收尾快照为 policy_version 7、gray/hourly=true、authoritative daily/manual=false。
- 预检错误：主目录问题记录明确记载 `instance_key` 字段错误、本机代理测试路径错误、中文路径编码失败、缺少时区/币种和 SSH 身份问题；生产台账 `2026-08-12 — babyboutiq.com 接入只读预检字段错误` 及其后续条目记录了对应止损与结果。

### 根本原因

现有证据不能量化 Google OAuth 页面等待时间，因此不能断言其耗时占比。能够确认的流程性根因是：接入申请未一次性收齐；历史文档和记忆被用于替代生产现场事实；基础资源、OAuth、policy、拉数、灰度、日报与 schedule 没有被明确建模为独立状态和授权阶段；命令缺少可复现、可短路的执行模板；后续重复交接环境没有真实隔离和固定版本。

### 止损、解决方案与正确顺序

发生错误时均停止扩大范围；schema、代理、Token 和 policy 问题在目标节点范围内纠正。schedule 产生额外任务后，仅禁用目标 schedule，不修改全局 scheduler、不强杀已认领任务，并等待终态核对。

统一正确顺序已经固化为：

```text
干净 worktree/Git 基线
→ 一次性接入申请和全阶段预演
→ 生产 SSH/systemd/Git/schema 只读核验
→ 专属代理生产出口验证
→ online backup + quick_check
→ API 分阶段创建基础资源并逐步保存 ID
→ onboarding policy 四开关关闭
→ OAuth URL/callback
→ credential validation
→ 仅切 lifecycle active
→ health check
→ 按独立授权开启 manual 并受控拉数
→ 按独立授权开启 gray/hourly 或 authoritative daily
→ 预演恢复候选日期
→ 最后创建/启用唯一 schedule 并观察两个周期
```

详细 payload、runtime、policy 版本断言、禁止事项和定向回滚以 `docs/production-ops-handoff-runbook.md` 为准。

### 验证结果、状态与防再犯

本次复盘为只读证据整理，没有连接或修改生产。2026-08-12 历史快照表明节点当时完成 OAuth 健康恢复，schedule 范围异常经过禁用、任务终态核对后恢复每小时计划；这些都不是当前实时状态证明。当前 account、OAuth、policy、schedule、任务和数据状态未在本次复盘中连接生产核验，必须在下一次操作前重新现场确认。

当前状态：已确认具体返工点及两段事件边界，但端到端总耗时和各阶段耗时无法从现有记录精确量化。流程性问题已通过干净交接 worktree、固定基线 Runbook 和接入前一次性预演清单整改。后续同类操作必须先检索本条；禁止重复旧字段、错误环境代理测试、猜测 Token、遗漏 lifecycle 门禁、未预演即启用 schedule，以及将新对话冒充新 worktree。

### 本次复盘文档操作失误

首次将复盘同时追加到三份文档时，维护台账锚点漏写一个空格，`apply_patch` 在校验阶段完整拒绝，三份文件均未产生部分写入，也没有生产影响。正确替代是读取目标文件尾部和精确段落后，使用短且唯一的锚点分别应用最小补丁。本次已按该方式纠正；再次证明长文档修改不得根据记忆重建锚点。

## 2026-08-13：维护手册追加式台账难以按问题类型检索

### 背景与现象

`docs/system-maintainer-onboarding-guide.md` 第 22 节按时间持续追加，能够保留审计时间线，但维护人员处理 OAuth、代理、调度、小时、日报、部署或节点接入问题时，需要顺序浏览整节才能找到相关记录。同一事故可能横跨多个领域，单纯重排或单选分类都会丢失上下文。

### 影响与根因

问题只影响文档检索效率，不影响运行代码或生产数据。根因是台账只有时间维度，没有独立导航层；若直接重排历史记录会破坏原始时间顺序，若复制正文到分类章节则会产生多个事实源和同步漂移。

### 解决方案与实施

保持全部正式记录标题和相对顺序不变，在记录模板后新增 11 个问题类型的分类索引；为每条正式记录增加唯一 ASCII HTML 锚点。索引只保存日期、标题、状态和链接，允许一条记录重复出现在多个分类中。后续新增台账必须同时增加锚点和至少一个分类索引项。

### 验证、状态与遗留风险

结构脚本已验证原有 15 条标题顺序不变、第 16 条仅追加、16 个锚点唯一、30 个链接有效、每条记录均被覆盖、11 类和状态词汇有效。首轮独立审阅发现三条尚待 Git/发布闭环的记录被误标为“已完成”；首次复审进一步发现“调度进程启动边界整改”在第二个分类中的重复索引状态未同步。现已把该记录的两处状态及另外两条状态统一改为“部分完成”，并规定只有目标、Git 和适用发布边界全部闭环才能使用“已完成”。最终定点复审确认 P0/P1/P2 均为 0，可以提交；实施提交为 `cd7be9e`，台账闭环提交紧随其后生成。当前状态：已解决。遗留风险是新增台账时忘记更新索引或同一记录在多分类中的状态不同步；防再犯措施是把索引、锚点、同锚点状态一致性和状态证据检查加入提交前验证，并在索引说明中明确同步和状态语义。

本次复验脚本出现六次无写入错误：前三次因错误分隔假设和边界定位触发一次 `IndexError`、两次 `ValueError`；第四次因 PowerShell 管道传入的中文 Python 源码编码差异触发状态断言失败；第五次继续在管道源码中使用中文文件名，触发 `OSError`；第六次错误断言只有问题记录包含 `IndexError`，忽略维护台账也必须记录同一错误。根因是未先固定编码无关的结构边界，并让自动断言依赖终端传入的中文常量、路径或不成立的唯一性假设；均未影响文档内容或生产。后续必须先用 `rg` 和逐行读取核对真实布局，从首条 `change-*` 锚点向上定位索引起点，自动断言只依赖 ASCII 结构，中文文件通过目录枚举定位，状态语义由 UTF-8 读取结果与独立复审确认。
